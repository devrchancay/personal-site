import Typography from 'typography';

const typography = new Typography({
  title: 'Poppins',
  baseFontSize: '16px',
  baseLineHeight: 1.2,
  headerFontFamily: ['Poppins', '-apple-system'],
  bodyFontFamily: ['Sintony', '-apple-system']
});

export default typography;
